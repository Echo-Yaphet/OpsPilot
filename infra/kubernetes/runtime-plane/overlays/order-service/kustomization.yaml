apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../../base]
nameSuffix: -order-service
labels:
  - pairs: {opspilot.io/target: order-service}
    includeSelectors: true
patches:
  - path: target.yaml
