apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../../base, data-pvc.yaml]
nameSuffix: -mysql
labels:
  - pairs: {opspilot.io/target: mysql}
    includeSelectors: true
patches:
  - path: target.yaml
