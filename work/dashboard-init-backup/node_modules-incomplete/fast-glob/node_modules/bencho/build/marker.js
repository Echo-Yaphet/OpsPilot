// out/main/constants.js
var BENCHO_MARKER_PREFIX = "bencho_marker";
var BENCHO_MARKER_RE = new RegExp(`${BENCHO_MARKER_PREFIX}:(?<data>.+)`, "i");

// out/types/common.js
var MarkerType;
(function(MarkerType2) {
  MarkerType2["Time"] = "time";
  MarkerType2["Memory"] = "memory";
  MarkerType2["Value"] = "value";
})(MarkerType = MarkerType || (MarkerType = {}));

// out/marker/marker.js
function time(label, value2) {
  log(label, MarkerType.Time, value2);
}
function memory(label, value2) {
  log(label, MarkerType.Memory, value2);
}
function value(label, value2) {
  log(label, MarkerType.Value, value2);
}
function log(label, type, value2) {
  console.log(formatMarker(label, type, value2));
}
var MarkerManager = class {
  #markers = [];
  time(label, value2) {
    this.#markers.push(formatMarker(label, MarkerType.Time, value2));
  }
  memory(label, value2) {
    this.#markers.push(formatMarker(label, MarkerType.Memory, value2));
  }
  value(label, value2) {
    this.#markers.push(formatMarker(label, MarkerType.Value, value2));
  }
  flush() {
    for (const marker of this.#markers) {
      console.log(marker);
    }
  }
};
function formatMarker(label, type, value2) {
  return `${BENCHO_MARKER_PREFIX}:${label};${type};${value2}`;
}
export {
  MarkerManager,
  log,
  memory,
  time,
  value
};
