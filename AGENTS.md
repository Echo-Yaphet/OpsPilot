# OpsPilot agent context

When continuing implementation, planning the next phase, or reporting project status, read [`PROJECT_STATUS.md`](PROJECT_STATUS.md) first. Treat its “Verified” and “Current limitations” sections as the handoff baseline, then verify any drift-prone runtime state with Docker Compose before acting.

